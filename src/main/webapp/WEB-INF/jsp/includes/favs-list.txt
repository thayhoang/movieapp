
<script
	src="${pageContext.request.contextPath}/scripts/user-remove-favs.js"></script>
	
<nav class="favs_list">
		<h2>Favourites</h2>
		<ul class="favs">
			<c:forEach var="fav" items="${favs}">
				<li id="fav_${fav.id}" title="${fav.description }"><a
					href="${pageContext.request.contextPath}/user/movies?id=${fav.id}"><c:out
							value="${fav.title }"></c:out></a></li>
			</c:forEach>
		</ul>
		<div class='trash'></div>
	</nav>